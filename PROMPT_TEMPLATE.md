# Master Prompt: ระบบสร้างแอปผู้ช่วยร่างหนังสือราชการแบบ Offline-First & Multi-Agent

> **คำแนะนำสำหรับการใช้งาน:**  
> คุณสามารถคัดลอก Prompt ด้านล่างนี้ทั้งหมด นำไปสั่ง AI Coding Assistant (เช่น Claude Code, Cursor, Antigravity, Codex, Windsurf ฯลฯ) ในโฟลเดอร์ว่าง เพื่อสร้างระบบผู้ช่วยร่างหนังสือราชการอัจฉริยะแบบ Offline-First ได้ทันที โดยสามารถปรับใช้กับหน่วยงานราชการ มหาวิทยาลัย หรือองค์กรใดก็ได้

---

```markdown
# Role & Goal
คุณคือ Senior Software Engineer และผู้เชี่ยวชาญด้านระบบเอกสารราชการไทย (Thai Government e-Document Architect) 
ภารกิจของคุณคือสร้าง **"ระบบผู้ช่วยร่างหนังสือราชการอัจฉริยะ (Offline-First Thai Official Document Agent Workspace)"** ขึ้นมาใน workspace นี้ตั้งแต่เริ่มต้น

ระบบนี้มีจุดเด่นคือ:
1. **Offline-First 100%:** ทำงานบนเครื่อง Local ทั้งหมด ไม่ต้องพึ่งพา Paid Cloud API ปลอดภัยต่อข้อมูลชั้นความลับของทางราชการ
2. **Preserve Real DOCX Formatting:** ไม่สร้างโครงสร้างเอกสารจากศูนย์ แต่ใช้วิธีคัดลอกไฟล์ DOCX Template ของจริงที่มีตราครุฑ ระยะกั้นหน้า/หลัง ระยะบรรทัด และสไตล์ แล้วแทนที่เฉพาะช่อง Placeholder `{{...}}` ทำให้รูปแบบเอกสารถูกต้องตามระเบียบงานสารบรรณ 100%
3. **Self-Learning Style & Tone:** มีระบบวิเคราะห์และเรียนรู้สำนวน คำขึ้นต้น คำลงท้าย โครงสร้างประโยค และทำเนียบผู้ลงนามจากตัวอย่างเอกสารจริงที่ผ่านการอนุมัติแล้ว
4. **Automated 6-Dimension Verifier:** ระบบตรวจสอบความถูกต้องก่อนส่งมอบ (ตรวจไฟล์ DOCX, ตรวจภาพตราครุฑ, ตรวจ Placeholder ตกค้าง, ตรวจส่วนหัว/ท้ายกระดาษ, ตรวจ Stylesheet, และตรวจเตือนข้อเท็จจริง/ตัวเลข)
5. **Interactive Web Dashboard:** หน้าเว็บแดชบอร์ดท้องถิ่น (Local Web UI) สำหรับสั่งงานด่วน ช่วยร่างเอกสารผ่านฟอร์มแบบโต้ตอบ ค้นหาตัวอย่างงาน และเปิดไฟล์ Word ได้ในคลิกเดียว
6. **Multi-Agent Coordination Protocol:** รองรับการทำงานร่วมกันระหว่างมนุษย์และ AI หลายตัวผ่านระบบ Lock, Work State JSON และสมุดส่งเวร (Handoff Log)

---

## 1. โครงสร้างโฟลเดอร์มาตรฐาน (Folder Contract)

ให้สร้างโครงสร้างโฟลเดอร์และไฟล์เริ่มต้นดังนี้:

```text
├── 01_templates/            # เก็บไฟล์ DOCX Template ทางการ (เช่น บันทึกข้อความภายใน, หนังสือภายนอก)
├── 02_current_work/         # เก็บไฟล์งานปัจจุบัน เช่น ข้อมูลดิบ ร่างคำสั่ง PDF/DOCX
├── 03_approved_examples/    # เก็บตัวอย่างเอกสารเก่าที่ได้รับการอนุมัติแล้ว เพื่อเรียนรู้สำนวน
│   ├── บันทึกข้อความภายใน/
│   ├── หนังสือภายนอก/
│   └── รายงานสรุป/
├── 04_generated/            # เก็บเอกสาร DOCX ผลลัพธ์ที่สร้างเสร็จแล้ว
├── knowledge/               # เก็บฐานความรู้และสถานะการทำงาน (สร้างโดยสคริปต์อัตโนมัติ)
│   ├── jobs/                # เก็บไฟล์ Job JSON กำหนดค่าการแทนที่ Placeholder
│   ├── locks/               # ระบบไฟล์ Lock ป้องกัน Agent ทำงานชนกัน
│   ├── file_index.json      # ดัชนีไฟล์ทั้งหมดใน Workspace
│   ├── template_profiles.json # ข้อมูล Placeholder และ Media ใน Template
│   ├── style_profile.json   # สรุปสไตล์ คำขึ้นต้น คำลงท้าย ทำเนียบผู้ลงนาม
│   ├── edit_history.json    # ประวัติ Diff การปรับแก้เอกสารด้วยมือ
│   └── agent_handoff.md     # สมุดบันทึกส่งเวรระหว่าง Agent
├── dashboard/               # เก็บไฟล์ index.html ของแดชบอร์ด
├── tools/                   # สคริปต์ Python สำหรับรันระบบแบบ Offline
│   ├── common.py            # ฟังก์ชันส่วนกลางและค่าคงที่
│   ├── update_knowledge.py  # วิเคราะห์ไฟล์ทั้งหมดและสร้าง Style/Template Profile
│   ├── docx_utils.py        # เครื่องมือแกะ/แทนที่ Placeholder ใน DOCX ผ่าน XML
│   ├── generate_from_template.py # สคริปต์สร้างเอกสารจาก Template + Job JSON
│   ├── verify_output.py     # สคริปต์ตรวจสอบความถูกต้อง 6 มิติ และ Fact-Check
│   ├── work_state.py        # จัดการสถานะงาน การ Claim งาน และระบบ Lock
│   ├── convert_doc_to_docx.py # แปลงไฟล์ .doc เก่าเป็น .docx
│   ├── build_dashboard.py   # คอมไพล์ข้อมูลเป็นหน้าเว็บ HTML Dashboard
│   └── dashboard_server.py  # Local HTTP Server สำหรับรับคำสั่งและเปิดเว็บ
├── agent-skills/            # Workflow มาตรฐานสำหรับ Agent
├── tests/                   # Automated Unit Tests
├── AGENTS.md                # กฎเหล็กและคู่มือสำหรับ AI Agent ทุกตัว
├── README.md                # คู่มือการใช้งานสำหรับผู้ใช้ทั่วไป
└── เปิดแดชบอร์ด.cmd         # ชอร์ตคัตแบบคลิกเดียวสำหรับเปิด Local Dashboard บน Windows
```

---

## 2. กฎระเบียบมาตรฐานงานสารบรรณ (Thai Official Document Rules)

ระบบและ Agent ต้องปฏิบัติตามกฎเหล่านี้อย่างเคร่งครัด:

1. **ไม่สร้าง Layout เองจากศูนย์ (Zero Layout from Scratch):** ให้คัดลอกไฟล์ต้นแบบจาก `01_templates/` เสมอ และแทนที่เฉพาะ Placeholder `{{...}}` เท่านั้น เพื่อไม่ให้ระยะบรรทัด ฟอนต์ และรูปตราครุฑเพี้ยน
2. **มาตรฐานตัวเลขไทย:** แปลงตัวเลขอารบิกในเนื้อหาภาษาไทยเป็นเลขไทย (`๑ ๒ ๓...`) เสมอ ยกเว้น URL, Email หรือรหัสภาษาอังกฤษ
3. **ภาคเหตุ (ย่อหน้า ๑):** ต้องระบุที่มาของหนังสือให้ครบถ้วน (เช่น ตามหนังสือ... ที่... ลงวันที่... ได้... หรือ ตามคำสั่ง... นั้น / ด้วย...) และ**ต้องลงท้ายย่อหน้าด้วยคำว่า `นั้น` เสมอ**
4. **ภาคความประสงค์ (ย่อหน้า ๒):** ขึ้นต้นด้วยคำเชื่อมทางการ เช่น `ในการนี้ [ชื่อหน่วยงาน] จึงใคร่ขอความอนุเคราะห์...` หรือ `บัดนี้ [ชื่อหน่วยงาน] ได้จัดทำ...เสร็จเรียบร้อยแล้ว จึงขอส่ง...` โดยให้ระบุรายละเอียดช่องทางติดต่อผู้ประสานงานท้ายย่อหน้านี้
5. **ภาคสรุป (ย่อหน้า ๓ / คำลงท้าย):** ต้องขึ้นบรรทัดใหม่แยกเดี่ยวตามแบบทางการเสมอ ห้ามรวมกับเนื้อหาติดต่อประสานงาน เช่น:
   - รายงาน/แจ้งเพื่อทราบ: `จึงเรียนมาเพื่อโปรดทราบ`
   - ขออนุมัติ/เสนอพิจารณา: `จึงเรียนมาเพื่อโปรดพิจารณา` / `จึงเรียนมาเพื่อโปรดพิจารณาอนุมัติ`
   - ขอความร่วมมือ/ข้อมูล: `จึงเรียนมาเพื่อโปรดพิจารณาให้ความอนุเคราะห์ จักขอบคุณยิ่ง`
   - ขอใช้สถานที่/ห้องประชุม: `จึงเรียนมาเพื่อโปรดพิจารณาอนุญาต`
   - เชิญประชุม: `จึงเรียนมาเพื่อโปรดเข้าร่วมประชุมตามวัน เวลา และสถานที่ดังกล่าว`
   - เสนอผู้บริหารลงนามหนังสือภายนอก: `จึงเรียนมาเพื่อโปรดพิจารณา หากเห็นชอบโปรดลงนามในหนังสือที่แนบมาพร้อมนี้`

---

## 3. รายละเอียดสคริปต์ใน `tools/`

### 3.1 `tools/docx_utils.py`
- จัดการไฟล์ `.docx` ด้วย `zipfile` และ `xml.etree.ElementTree`
- ฟังก์ชัน `find_docx_placeholders(path)`: ตรวจหา Placeholder รูปแบบ `{{...}}` ทั้งหมดในเนื้อหา, Header, Footer
- ฟังก์ชัน `replace_docx_placeholders(template_path, output_path, replacements)`: ทำการแทนที่ข้อความ Placeholder ใน XML โดยคงแท็กจัดรูปแบบ (Run/Paragraph properties) และความสมบูรณ์ของไฟล์ไว้ 100%

### 3.2 `tools/update_knowledge.py`
- สแกนไฟล์ทุกนามสกุลที่รองรับ (`.docx`, `.txt`, `.pdf`, `.md`) ในโฟลเดอร์หลัก
- วิเคราะห์ Template และสร้าง `knowledge/template_profiles.json`
- ดึงสไตล์จาก `03_approved_examples/` สรุปคำขึ้นต้น คำลงท้าย ทำเนียบผู้ลงนาม และสร้าง `knowledge/style_profile.json`
- อ่านสรุปงานปัจจุบันจาก `02_current_work/` สร้างเป็น `knowledge/current_work_brief.md`

### 3.3 `tools/generate_from_template.py`
- รับพารามิเตอร์ `--template`, `--job`, `--agent`
- ตรวจสอบว่า Placeholder ใน Template ถูกระบุใน Job JSON ครบถ้วนหรือไม่ หากขาดตัวใดให้แจ้งเตือนทันที
- สั่งสร้างไฟล์ DOCX ผลลัพธ์ลงใน `04_generated/`
- อัปเดต `knowledge/work_state.json` และ `knowledge/agent_handoff.md`

### 3.4 `tools/verify_output.py`
- ตรวจสอบไฟล์ผลลัพธ์เทียบกับ Template 6 มิติ:
  1. `valid_docx`: เป็นไฟล์ DOCX ที่สมบูรณ์ เปิดได้ไม่เสียหาย
  2. `missing_media`: ตราครุฑและรูปภาพจาก Template ยังคงอยู่ครบ
  3. `unresolved_placeholders`: ไม่มี Placeholder `{{...}}` ตกค้างในเอกสาร
  4. `missing_headers`: Header ไม่สูญหาย
  5. `missing_footers`: Footer ไม่สูญหาย
  6. `missing_styles`: สไตล์เอกสารยังอยู่ครบ
- **Fact-Check Warning:** ตรวจเทียบตัวเลขในเอกสารผลลัพธ์กับเอกสารต้นเรื่อง หากพบตัวเลขที่ไม่ตรงกับต้นเรื่องให้แจ้งเตือน
- **Diff Tracking:** หากผู้ใช้แก้ไขไฟล์ DOCX ด้วยมือ ระบบจะคำนวณความต่าง (Diff) และบันทึกลง `knowledge/edit_history.json` เพื่อให้ AI เรียนรู้ในการร่างครั้งถัดไป

### 3.5 `tools/dashboard_server.py` & `tools/build_dashboard.py`
- คอมไพล์ข้อมูลทั้งหมดเป็นหน้าเว็บ Dashboard สวยงาม ทันสมัย รองรับภาษาไทย (Google Fonts: Sarabun / Prompt)
- รัน Local Web Server (เช่น พอร์ต `8765`) มี API ให้หน้าเว็บสั่ง:
  - `/api/update-knowledge`: อัปเดตฐานความรู้
  - `/api/generate-doc`: รับข้อมูลจากฟอร์มแล้วสร้าง DOCX พร้อม Verify ทันที
  - `/api/open-file`: สั่งเปิดไฟล์ DOCX ใน Microsoft Word หรือเปิดโฟลเดอร์บนเครื่อง
  - `/api/verify-doc`: สั่งตรวจความถูกต้องของเอกสาร

---

## 4. ฟีเจอร์หน้าเว็บ Interactive Dashboard

สร้าง Dashboard (`dashboard/index.html`) ให้มีฟังก์ชันดังนี้:
1. **แท็บภาพรวม (Overview):** การ์ดแสดงจำนวนไฟล์ ปุ่มสั่งงานด่วน และรายการ Template พร้อมใช้งาน
2. **แท็บผู้ช่วยร่างเอกสาร (Interactive Drafter):** 
   - ปุ่มเลือกแม่แบบงานด่วน (Presets) เช่น ขอข้อมูล, ขออนุมัติ, เชิญประชุม, ขอใช้ห้อง, ส่งเล่มรายงาน
   - ฟอร์มกรอกข้อมูลตาม Placeholder พร้อมระบบ Auto-complete ทำเนียบผู้ลงนามและคำลงท้าย
   - ปุ่มแปลงเลขอารบิกเป็นเลขไทยในคลิกเดียว
   - ปุ่มกดสร้าง DOCX ทันที พร้อมปุ่มเปิดไฟล์ใน Word
3. **แท็บคลังตัวอย่างงาน (Approved Examples):** ช่องค้นหาตัวอย่างเอกสารเก่าแบบ Real-time พร้อมปุ่มคัดลอกข้อความไปใช้ในฟอร์มร่าง
4. **แท็บเอกสารที่สร้างแล้ว (Generated Documents):** ตารางแสดงไฟล์ DOCX ผลลัพธ์ ปุ่มเปิดใน Word และปุ่มตรวจเอกสาร
5. **แท็บทำเนียบและสำนวนมาตรฐาน (Style Guide):** ตารางแสดงรายชื่อผู้ลงนาม ตำแหน่ง และสูตรประโยคมาตรฐาน
6. **แท็บประวัติและส่งเวร (Handoff & Edit History):** แสดงสมุดส่งเวรระหว่าง AI และประวัติการปรับแก้ด้วยมือ

---

## 5. เริ่มต้นสร้าง Workspace

กรุณาเริ่มสร้างไฟล์ทั้งหมดตามโครงสร้างด้านบน พร้อมสร้างไฟล์ตัวอย่าง Template DOCX จำลอง, สคริปต์ Python ทุกตัว, ชุดทดสอบ Unit Test (`tests/`), และไฟล์คำสั่ง `เปิดแดชบอร์ด.cmd` ให้ครบถ้วนสมบูรณ์ พร้อมทดสอบรัน Unit Test ให้ผ่านทุกข้อ
```
